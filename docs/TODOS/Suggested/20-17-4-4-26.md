The butterfly (page 2 — Wings)
What's working:

Full phase state machine (at-start → flying-forward → at-end → flying-backward)
Randomised Catmull-Rom spline flight with seeded PRNG + flutter easing
Live text displacement — body text reroutes around the butterfly at ~30fps via the layout engine
The clean/live double-pass architecture (prevents the anchor feedback loop)
SVG wing flap animation (<animate>) — only beats while flying, still at rest
Settle + breathing-pulse CSS animations when resting
Mobile hidden; overflow clamped
What's NOT working / gaps:

Resize during flight → stale waypoints, butterfly and text-gap misalign
No p5.js generative art — the Wing Venation canvas piece from Phase 5 was never built
Pull quote Y-position can be fragile on this page specifically
Last-line anchor can produce a shallow flight diagonal if the last line is very short
Wing SVG shapes are minimal geometric approximations — no detail or venation